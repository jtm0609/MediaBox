pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "knowmerceAssignment"
include(":app")
include(":data")
include(":presentation")
include(":domain")
include(":data-local")
include(":data-remote")
